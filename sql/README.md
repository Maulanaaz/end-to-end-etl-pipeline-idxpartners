# 🗄️ Database Scripts & Architecture

This directory contains the SQL scripts required to build the Data Warehouse infrastructure (Snowflake Schema) and set up the automation logic.

## 📂 Folder Structure

* **`ddl/` (Data Definition Language):** Scripts to create table structures (`CREATE TABLE`).
* **`sp/` (Stored Procedures):** Scripts to define business logic and automation (`CREATE PROCEDURE`).
